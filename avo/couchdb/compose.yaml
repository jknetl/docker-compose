version: '3'
services:
  couchdb:
    image: couchdb:3
    ports:
      - "5984:5984"
    environment:
      - COUCHDB_USER=admin
    env_file:
      - .env.secrets
    volumes:
        - ./data:/opt/couchdb/data
networks:
  default:
    external: true
    name: ingress